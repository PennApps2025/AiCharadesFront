// RoundIntro was used for per-round countdowns in an earlier design.
// This project now uses a single global match timer. Export a harmless stub
// to avoid import errors in case any files still reference it.

const RoundIntro = () => null;

export default RoundIntro;
